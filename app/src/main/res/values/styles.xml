<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:fastScrollThumbDrawable">@drawable/fastscroll_thumb</item>
        <item name="android:fastScrollPreviewBackgroundRight">@drawable/fastscroll_bubble</item>
        <item name="android:fastScrollOverlayPosition">atThumb</item>
        <item name="android:fastScrollTextColor">#FFFFFF</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Dark"/>

    <style name="AppTheme.AlertDialog" parent="Theme.AppCompat.Dialog.Alert">
        <item name="android:windowTitleStyle">@style/AlertDialogTitleStyle</item>
        <item name="colorPrimary">@color/colorPrimaryLight</item>
        <item name="colorAccent">@color/colorPrimaryLight</item>
    </style>

    <style name="AppTheme.ProgressDialog" parent="Theme.AppCompat.Dialog">
        <item name="colorAccent">@color/colorPrimaryLight</item>
        <item name="android:textColorPrimary">@color/colorPrimaryLight</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <style name="ActionBarStyle" parent="@style/Widget.AppCompat.ActionBar.Solid">
        <item name="height">70dp</item>
    </style>

    <style name="ToolbarStyle" parent="@style/ThemeOverlay.AppCompat">
        <item name="android:textColorSecondary">#FFFFFF</item>
    </style>

    <style name="AlertDialogTitleStyle" parent="@style/TextAppearance.AppCompat.Title">
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/colorPrimaryLight</item>
    </style>

    <style name="NavigationViewStyle">
        <item name="android:listDivider">@color/navigation_drawer_divider</item>
        <item name="android:scrollbarThumbVertical">@android:color/transparent</item>
    </style>

</resources>
